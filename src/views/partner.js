import * as React from "react"
import Layout from "../components/layout"

const Partner = ({ pageContext: { locale } }) => {

  return (
    <Layout id="partner" locale={locale}>
      Partner
    </Layout>
  )
}

export default Partner
