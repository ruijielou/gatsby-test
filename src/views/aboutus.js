import * as React from "react"
import Layout from "../components/layout"

const Aboutus = ({ pageContext: { locale } }) => {

  return (
    <Layout id="aboutus" locale={locale}>
      Aboutus
    </Layout>
  )
}

export default Aboutus